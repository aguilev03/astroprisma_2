{
  "name": "astroprisma",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:less",
    "build:less": "lessc less/astroprisma.less css/astroprisma.css",
    "release": "npm run build && node scripts/build-release.mjs"
  },
  "author": "molilo",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "less": "^4.2.0"
  }
}
